# Reorganize project dissections by learning path

Open-source project dissections will be organized by learning path rather than by the original repository directory layout. Each module should still record the source files it maps to, so the learner can move between conceptual practice modules and the real project implementation.

Each open-source project gets its own directory under `projects/`, while general ML/DL handwriting modules live separately in `ml_dl_handwriting/`.

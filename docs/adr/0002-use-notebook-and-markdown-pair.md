# Use notebook and markdown pairs for learning modules

Each learning module will keep both an interactive notebook and a Markdown reading version. The notebook is the primary practice surface because it supports TODO cells, local execution, tests, and `STOP HERE` answer separation; the Markdown version keeps the same content easy to browse, review, and publish.


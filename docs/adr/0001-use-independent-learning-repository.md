# Use an independent learning repository

We will build this as an independent personal learning repository instead of continuing inside the upstream `llm-algo-leetcode` fork. The project scope includes interview handwriting drills, SFT/DPO training topics, source-derived exercises, and open-source project dissection, which would blur the purpose of a fork meant for upstream pull requests.

